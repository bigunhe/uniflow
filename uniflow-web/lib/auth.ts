/**
 * Central auth — ONLY Member 4 edits this file.
 * All feature silos MUST use getCurrentUser() for auth checks.
 * Do not add custom Supabase auth calls inside route groups.
 */

export async function getCurrentUser() {
  // Placeholder: Member 4 implements with Supabase
  return null;
}
